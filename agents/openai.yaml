# Optional Codex app metadata. The portable skill entrypoint remains ../SKILL.md.
interface:
  display_name: "D2 Diagrams"
  short_description: "Create, convert, validate, and render D2 diagrams."
  brand_color: "#2563EB"
  default_prompt: "Use the d2-diagrams skill to create clear, maintainable D2 source and render it when the CLI is available."
policy:
  allow_implicit_invocation: true
